<!DOCTYPE html>
<html lang="ru">
<head>
	<meta charset="UTF-8">
	<title>Hello World!</title>
</head>
<body>
	<ul>
		{{range $key, $value := .}}
		<li>{{$key}} - {{$value}}</li>
		{{end}}
	</ul>
</body>
</html>
